name: CI

on:
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      proxy: ${{ steps.filter.outputs.proxy }}
      proxy-consumer: ${{ steps.filter.outputs.proxy-consumer }}
      web: ${{ steps.filter.outputs.web }}
      utils: ${{ steps.filter.outputs.utils }}
      types: ${{ steps.filter.outputs.types }}
      convex: ${{ steps.filter.outputs.convex }}
      root: ${{ steps.filter.outputs.root }}
      workflows: ${{ steps.filter.outputs.workflows }}
    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            proxy:
              - 'workers/proxy/**'
              - 'packages/utils/**'
              - 'packages/types/**'
            proxy-consumer:
              - 'workers/proxy-consumer/**'
              - 'packages/utils/**'
              - 'packages/types/**'
            web:
              - 'workers/web/**'
              - 'packages/utils/**'
              - 'packages/types/**'
            utils:
              - 'packages/utils/**'
            types:
              - 'packages/types/**'
            convex:
              - 'convex/**'
            root:
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'turbo.json'
              - 'eslint.config.js'
              - '.prettierrc'
              - '.prettierignore'
              - 'tsconfig.json'
            workflows:
              - '.github/workflows/**'

  actionlint:
    name: Lint GitHub Actions
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.workflows == 'true'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: devops-actions/actionlint@v0.1.3

  utils:
    name: Utils Package
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.utils == 'true' || needs.changes.outputs.root == 'true'
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Cache Turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm prettier --check "packages/utils/**/*.{ts,tsx,js,jsx,json}"

      - name: Lint
        run: pnpm turbo run lint --filter=@trace-flow/utils

      - name: Type check
        run: pnpm turbo run type-check --filter=@trace-flow/utils

      - name: Test
        run: pnpm turbo run test --filter=@trace-flow/utils

  types:
    name: Types Package
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.types == 'true' || needs.changes.outputs.root == 'true'
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Cache Turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm prettier --check "packages/types/**/*.{ts,tsx,js,jsx,json}"

      - name: Lint
        run: pnpm turbo run lint --filter=@trace-flow/types

      - name: Type check
        run: pnpm turbo run type-check --filter=@trace-flow/types

  convex:
    name: Convex Backend
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.convex == 'true' || needs.changes.outputs.root == 'true'
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm prettier --check "convex/**/*.{ts,tsx,js,jsx,json}"

      - name: Lint
        run: pnpm turbo run lint --filter=@trace-flow/convex

      - name: Type check
        run: pnpm turbo run type-check --filter=@trace-flow/convex

      - name: Test
        run: pnpm -C convex test

  proxy:
    name: Proxy Worker
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.proxy == 'true' || needs.changes.outputs.root == 'true'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Cache Turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm prettier --check "workers/proxy/**/*.{ts,tsx,js,jsx,json}"

      - name: Lint
        run: pnpm turbo run lint --filter=@trace-flow/proxy

      - name: Type check
        run: pnpm turbo run type-check --filter=@trace-flow/proxy

      - name: Test with coverage
        run: pnpm turbo run test:coverage --filter=@trace-flow/proxy

      - name: Report coverage
        if: github.event_name == 'pull_request'
        uses: davelosert/vitest-coverage-report-action@v2
        with:
          working-directory: workers/proxy
          json-summary-path: coverage/coverage-summary.json
          json-final-path: coverage/coverage-final.json
          file-coverage-mode: changes

      - name: Build
        run: pnpm turbo run build --filter=@trace-flow/proxy

  proxy-consumer:
    name: Proxy Consumer Worker
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.proxy-consumer == 'true' || needs.changes.outputs.root == 'true'
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Cache Turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm prettier --check "workers/proxy-consumer/**/*.{ts,tsx,js,jsx,json}"

      - name: Lint
        run: pnpm turbo run lint --filter=@trace-flow/proxy-consumer

      - name: Type check
        run: pnpm turbo run type-check --filter=@trace-flow/proxy-consumer

      - name: Test with coverage
        run: pnpm turbo run test:coverage --filter=@trace-flow/proxy-consumer

      - name: Report coverage
        if: github.event_name == 'pull_request'
        uses: davelosert/vitest-coverage-report-action@v2
        with:
          working-directory: workers/proxy-consumer
          json-summary-path: coverage/coverage-summary.json
          json-final-path: coverage/coverage-final.json
          file-coverage-mode: changes

      - name: Build
        run: pnpm turbo run build --filter=@trace-flow/proxy-consumer

  web:
    name: Web Worker
    runs-on: ubuntu-latest
    needs: [changes]
    if: needs.changes.outputs.web == 'true' || needs.changes.outputs.root == 'true'
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Cache Next.js
        uses: actions/cache@v4
        with:
          path: workers/web/.next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ hashFiles('**/*.[jt]s', '**/*.[jt]sx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-${{ hashFiles('**/pnpm-lock.yaml') }}-

      - name: Cache Turbo
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm prettier --check "workers/web/**/*.{ts,tsx,js,jsx,json,css}"

      - name: Lint
        run: pnpm turbo run lint --filter=@trace-flow/web

      - name: Type check
        run: pnpm turbo run type-check --filter=@trace-flow/web

      - name: Test
        run: pnpm turbo run test --filter=@trace-flow/web

      - name: Build
        env:
          NEXT_PUBLIC_CONVEX_URL: ${{ vars.NEXT_PUBLIC_CONVEX_URL }}
          NEXT_PUBLIC_AUTH0_DOMAIN: ${{ vars.NEXT_PUBLIC_AUTH0_DOMAIN }}
          NEXT_PUBLIC_AUTH0_CLIENT_ID: ${{ vars.NEXT_PUBLIC_AUTH0_CLIENT_ID }}
          NEXT_PUBLIC_TINYBIRD_API_URL: ${{ vars.NEXT_PUBLIC_TINYBIRD_API_URL }}
        run: pnpm turbo run build --filter=@trace-flow/web

  status:
    name: CI Status
    runs-on: ubuntu-latest
    if: always()
    needs:
      - changes
      - actionlint
      - utils
      - types
      - convex
      - proxy
      - proxy-consumer
      - web
    steps:
      - name: Check job statuses
        run: |
          if [[ "${{ contains(needs.*.result, 'failure') }}" == "true" ]]; then
            echo "One or more jobs failed"
            exit 1
          elif [[ "${{ contains(needs.*.result, 'cancelled') }}" == "true" ]]; then
            echo "One or more jobs were cancelled"
            exit 1
          else
            echo "All jobs completed successfully"
          fi
