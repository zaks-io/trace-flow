name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]
  pull_request_review:
    types: [submitted]

jobs:
  claude:
    runs-on: ubuntu-latest
    if: |
      !contains(github.event.comment.body, '/review')
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Claude Code
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          track_progress: true
          claude_args: "--allowedTools 'Edit,MultiEdit,Write,Read,Glob,Grep,LS,Bash(git:*),Bash(bun:*),Bash(npm:*),Bash(npx:*),Bash(gh:*)'"

  code-review:
    name: Manual Code Review
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'issue_comment' &&
      github.event.issue.pull_request &&
      contains(github.event.comment.body, '/review')
    permissions:
      contents: read
      pull-requests: write
      id-token: write
      actions: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Claude Code Review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          track_progress: true
          claude_args: |
            --allowedTools "Read,Grep,Glob,LS,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh run list:*),Bash(gh run view:*)"
          prompt: |
            **PR Context:**
            - Repo: ${{ github.repository }}
            - PR Number: ${{ github.event.issue.number }}
            - Triggered by: Manual `/review` comment

            A PR review has been triggered. Review the current state of the PR
            to determine if a review has already occured. If it has, review the
            feedback and any changes made since then to see if they address the
            feedback of the original review. Your purpose is to continue the
            review and not start over. If no review has occured, perform a 
            comprehensive code review based on the following focus areas:

            1. **Code Quality**
               - Clean code principles and best practices
               - Proper error handling and edge cases
               - Code readability and maintainability
               - Code follows KISS and DRY principles
               - Consistenty with the project style and conventions

            2. **Security**
               - Check for potential security vulnerabilities
               - Validate input sanitization
               - Review authentication/authorization logic
               - Verify no secrets are commited to the repository

            3. **Performance**
               - Identify potential performance bottlenecks
               - Review database queries for efficiency
               - Check for memory leaks or resource issues
               - Follows serverless best practices

            4. **Testing & Coverage**
               - Verify adequate test coverage
               - Review test quality and edge cases
               - Check for missing test scenarios
               - Analyze reports via: gh run view <run-id> --log
               - Look for critical coverage regressions or gaps

            5. **Documentation**
               - Ensure code is properly documented
               - Verify README updates for new features
               - Check API documentation accuracy

            Provide focused feedback using inline comments for specific issues.
            Use top-level comments for general observations.

            Be witter, engaging, concise and to the point. Do not repeat
            comments made by previous reviewers. Your report should focus
            on improvements and not praise.
