[package]
name = "collector-contracts"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Wire contract types for the Trace Flow agent Collector ingest envelope. Mirrors packages/types/src/agent-ingest.ts."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
