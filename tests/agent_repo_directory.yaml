# Fixtures: fixtures/agent_message_facts.ndjson (org_repo).
# org_repo repos at 2026-05-20 10:00:
#   fp_gh    -> normalized_git_remote "github.com/acme/widgets"   (client label: acme/widgets)
#   fp_local -> repo_path_fallback "/Users/dev/projects/scratch-pad" (client label: local: scratch-pad)
#   fp_bare  -> neither (client label: fingerprint prefix)
# Name derivation is client-side (see agentMetrics/repo tests); this asserts the raw fields.
# retention_days=20000 neutralizes the now()-relative tier floor (production passes 7..365).

- name: directory_returns_repo_fields_per_fingerprint
  description: one row per repo_fingerprint with the remote + path-fallback fields the client resolves
  expected_http_status: 200
  parameters: org_id=org_repo&start_time_ms=1779235200000&end_time_ms=1779321600000&retention_days=20000
  expected_result: |
    {"repo_fingerprint":"fp_bare","normalized_git_remote":"","repo_path_fallback":"","repo_source":"remote"}
    {"repo_fingerprint":"fp_gh","normalized_git_remote":"github.com\/acme\/widgets","repo_path_fallback":"","repo_source":"remote"}
    {"repo_fingerprint":"fp_local","normalized_git_remote":"","repo_path_fallback":"\/Users\/dev\/projects\/scratch-pad","repo_source":"path"}
