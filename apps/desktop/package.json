{
  "name": "@trace-flow/desktop",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "rust:check": "cargo check --manifest-path=./src-tauri/Cargo.toml --locked",
    "rust:clippy": "cargo clippy --manifest-path=./src-tauri/Cargo.toml --all-targets --locked -- -D warnings",
    "rust:fmt": "cargo fmt --manifest-path=./src-tauri/Cargo.toml -- --check",
    "rust:test": "cargo test --manifest-path=./src-tauri/Cargo.toml --locked"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
