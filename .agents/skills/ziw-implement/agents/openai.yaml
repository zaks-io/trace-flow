interface:
  display_name: "Implement"
  short_description: "Implement, review, and PR"
  default_prompt: "Use $ziw-implement to implement exactly this tracker issue, respect its out-of-scope boundary, run code review, iterate, and create the PR."
policy:
  allow_implicit_invocation: false
