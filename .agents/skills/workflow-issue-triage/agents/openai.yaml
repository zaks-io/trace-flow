interface:
  display_name: "Issue Triage"
  short_description: "Shape tracker tickets"
  default_prompt: "Use $workflow-issue-triage to triage and update issue tracker projects, labels, dependencies, orphans, ready-for-agent status, and unclear tickets that need human next actions."
policy:
  allow_implicit_invocation: false
