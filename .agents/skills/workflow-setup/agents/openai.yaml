interface:
  display_name: "Setup"
  short_description: "Create or refresh config"
  default_prompt: "Use $workflow-setup to create or refresh this repo's agent workflow config by reading any existing config first."
policy:
  allow_implicit_invocation: false
