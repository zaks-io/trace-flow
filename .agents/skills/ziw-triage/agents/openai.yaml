interface:
  display_name: "Issue Triage"
  short_description: "Shape tracker tickets"
  default_prompt: "Use $ziw-triage to triage current project tracker issues, make Todo tickets ready for agents, reconcile verified stale states, update labels, dependencies, orphans, and clean requested backlog or intake issues."
policy:
  allow_implicit_invocation: false
