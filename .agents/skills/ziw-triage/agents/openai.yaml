interface:
  display_name: "Issue Triage"
  short_description: "Shape tracker tickets"
  default_prompt: "Use $ziw-triage to triage current project tracker issues, make Todo tickets ready for agents, reconcile verified stale states, update labels, estimates only when the configured estimate policy grants triage that step, dependencies, orphans, and clean explicitly requested Linear Backlog review or backfill scope separately from configured intake issues."
policy:
  allow_implicit_invocation: false
